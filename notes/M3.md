# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._

## Step 2 — CLAUDE.md behaviors

- It forbids editing tests to make them pass — without this, Claude will absolutely do that since it's the easiest path to green.
- It tells Claude to run `pytest` after each change — without this, Claude would batch all edits and only discover breakage at the end.
- It says keep changes scoped to the task — without this, Claude will opportunistically refactor unrelated code it notices along the way.
